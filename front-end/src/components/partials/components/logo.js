import React from "react";
import logo from "./logo.png";
// import { stepClasses } from '@mui/material'
const Logo = (props) => {
  return <img src={logo} alt="Logo" style={{ width: 207 }} height={55} />;
};

export default Logo;
